SERVER = A program that :
1: waits for requests,
2:process them,
3:and sends back the responses

Node:
   A run time envirnment for JavaScript

Packages:
   A packages is a ready-made piece of code written by some one else/A packages is a reusable piece of code which is installed via npm for faster development 

 Note:
    The packages code is inside the node module folder 

    NodeJS application:
      A node js application is the program that is written in javascript code and run in server side 

 Modules:
      Modules is a single file of code ,instead of to create everything in a single file we break the code into files these files are called modules 
      :we have some buildIn modules:
      fs modules,
      http,
      path,
      no installation require 

      and some modules must be installed before using 

   file Handelling:
     file handelling means creating , updating , deleting and managing files 

     it done with fs module